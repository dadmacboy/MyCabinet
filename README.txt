FileNest — single-file scan build

A scan now produces exactly one document:
- PDF selected: one .pdf
- JPEG selected: one .jpg

The optional filing-record export control has been removed.
The Share Sheet receives only the document file, with no accompanying text payload.
Scanner detection and PDF/JPEG creation are unchanged.
